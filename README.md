# Binary-Search-for-Search-Insert-Position-in-a-Sorted-Array
This solution applies binary search to locate a target in a sorted array of distinct integers. If the target exists, its index is returned. Otherwise, the algorithm returns the correct insertion index while maintaining sorted order. It runs in O(log n) time and uses O(1) extra space.
